# Legacy SQL

Arquivos preservados apenas como referencia historica. **Nao executar em
producao.** A versao autoritativa do schema esta em `supabase/migrations/`,
incluindo o hardening em `20260426180000_security_hardening.sql`.

- `supabase-schema.sql` — primeira versao (continha `role default 'manager'`).
- `supabase-schema-v2.sql` — iteracao intermediaria.
