import { PlaceholderPage } from '@/components/placeholder-page';

export default function ConfiguracoesPage() {
  return (
    <PlaceholderPage
      title="Configurações"
      description="Gestão de usuários do sistema (admin, gestor, SSMA)."
      phase="fase futura"
    />
  );
}
